spring:
  application:
    name: eureka-service

server:
  port: 8761
  address: 0.0.0.0

eureka:
  client:
    # El propio Eureka no se registra a si mismo
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: ${EUREKA_HOSTNAME:localhost}
